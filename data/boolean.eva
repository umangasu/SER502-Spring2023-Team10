program {
    bool x;
    x = true;
    bool y;
    y = true;
    if (x == false) {
        print("x is", x);
    } else if (y == false) {
        print("y is", y);
    } else {
        print("This is else part");
    }
}